[project]
name = "sipx-softphone"
version = "0.1.0"
description = "Headless native softphone app package for sipx"
requires-python = ">=3.14"
dependencies = ["sipx"]

[tool.uv.sources]
sipx = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
