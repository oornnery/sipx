[project]
name = "sipx-tts"
version = "0.1.0"
description = "TTS adapter slot for sipx media protocols"
requires-python = ">=3.14"
dependencies = ["sipx"]

[tool.uv.sources]
sipx = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
