[sipx-lab]
exten => 1000,1,NoOp(sipx native UAS quick answer)
 same => n,Answer()
 same => n,Wait(1)
 same => n,Hangup()

exten => 1001,1,NoOp(sipx native UAS waits for caller BYE)
 same => n,Answer()
 same => n,Wait(30)
 same => n,Hangup()

exten => 2000,1,NoOp(sipx ARI Stasis target)
 same => n,Stasis(sipx,inbound)
 same => n,Hangup()
