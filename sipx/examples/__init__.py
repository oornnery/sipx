"""Runnable SIP examples built only on the public ``sipx`` API.

Each example is a small async script configured through ``SIPX_*`` environment
variables (see ``sipx.examples.common``) so it can run against a real SIP
server without command-line parsing.

References:
    RFC 3261 - SIP: Session Initiation Protocol (methods demonstrated)
"""
